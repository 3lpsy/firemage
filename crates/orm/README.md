SeaORM entity definitions for persisted Firemage resources.

- Models: users, credentials, VMs, networks, activity, secrets, kernel aliases, files and snapshots.
- Egress models: upstream proxies, reusable policies and VM policy bindings.
- JSON specifications retain typed application settings; ownership, aliases, revisions and catalog references have dedicated columns.
