# app

Desktop WASM entry point, sidebar shell, and purple visual system.

- `app` crate in the desktop web UI.
- Plain Cargo plus wasm-bindgen; assets load from the same origin.
- Desktop layout targets 1280–1920 px.
