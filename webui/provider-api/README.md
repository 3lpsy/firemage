# provider-api

Same-origin JSON requests with cookie credentials and CSRF headers.

- `provider` crate in the desktop web UI.
- Relative `/v1/` paths only; no bearer tokens in browser storage.
- Returns server error messages with HTTP status codes.
