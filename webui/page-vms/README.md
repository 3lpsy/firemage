# page-vms

VM inventory with live refresh and a selected-machine inspector.

- `page` crate in the desktop web UI.
- Rendered by the authenticated app shell.
- Mutations use the server session and CSRF token.
