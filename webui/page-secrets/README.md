Administrator secrets page at `/#secrets`.

- Lists only the signed-in owner’s names and update timestamps.
- Creates and replaces write-only values; never fetches stored values.
- Explains the difference between host-side proxy credentials and secrets delivered to a guest.
