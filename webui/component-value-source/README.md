Shared control for a plain value or a named secret reference.

- Uses radio choices and an owner-scoped secret selector.
- Never reads secret values; literal credential inputs are masked by default.
- Optional prefixes support injected authorization headers.
