# view-vm-detail

VM inspector with lifecycle controls, egress, environment, boot inputs, logs, output files, metadata, and raw Firecracker access.

- `view` crate in the desktop web UI.
- Shared by the authenticated app shell and its resource pages.
